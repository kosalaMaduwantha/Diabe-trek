import 'package:flutter/material.dart';

class WoundHistory extends StatelessWidget {
  const WoundHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Wound History')),
      body: const Center(
        child: Text('Page 2'),
      ),
    );
  }
}
